/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ChooseLocationsComponent } from './choose-locations.component';

describe('Component: ChooseLocations', () => {
  it('should create an instance', () => {
    let component = new ChooseLocationsComponent();
    expect(component).toBeTruthy();
  });
});
